---
import Layout from '../layouts/Page.astro'
import Container from '../components/Container.astro';
import BlogPostsList from '../components/BlogPostsList.astro';

const posts = (await Astro.glob('./blog/*.{md,mdx}')).sort(
	(a, b) => new Date(b.frontmatter.publishedAt).valueOf() - new Date(a.frontmatter.publishedAt).valueOf()
);

const seo = {
  title: 'Autana Muebles | store and blog',
  description: 'Autana Muebles, fabricación de muebles a pedido y a la medida',
}
---

<style>
  .home-hero-text__container {
    padding: 5vh 0;
  }
  h1 {
    text-align: center;
  }
</style>

<Layout seo={seo}>
  <Container>
    <div class="home-hero-text__container">
      <h2 class="fade-in">Bienvenidos</h2>
      <h4 class="fade-in">Autana Muebles</h4>
    </div>
    <BlogPostsList posts={posts}/>
  </Container>
</Layout>

